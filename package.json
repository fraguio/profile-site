{
  "name": "profile-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19 <21"
  },
  "scripts": {
    "dev": "astro dev",
    "astro": "astro",
    "build": "npm run validate:contract && astro build",
    "validate:contract": "node ./scripts/validate-resume-contract.mjs",
    "deps:ci": "npm ci"
  },
  "devDependencies": {
    "astro": "^5.13.5"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
