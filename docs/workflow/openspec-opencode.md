# Workflow OpenSpec + OpenCode (contexto histórico)

Este documento conserva el contexto de evolución del flujo con overlays `ospec-*` sobre comandos upstream `opsx-*`.

## Referencia operativa vigente

Para ejecución diaria, usar exclusivamente:

- `docs/workflow/openspec-simple-skills-flow.md`

## Qué aporta este documento

- Registro de la decisión de mantener un flujo simple para `profile-site`.
- Contexto de por qué se integraron `tdd` e `improve-codebase-architecture` dentro del flujo OpenSpec.
- Contexto de la estrategia de overlays locales (`ospec-*`) frente a comandos upstream (`opsx-*`).

## Regla de mantenimiento

- No duplicar aquí reglas operativas que ya estén en el flujo canónico.
- Si se actualiza la operativa, modificar primero `docs/workflow/openspec-simple-skills-flow.md`.
