# Workflow OpenSpec + OpenCode (contexto historico)

Este documento conserva el contexto de evolucion del flujo con overlays `ospec-*` sobre comandos upstream `opsx-*`.

## Referencia operativa vigente

Para ejecucion diaria, usar exclusivamente:

- `docs/workflow/openspec-simple-skills-flow.md`

## Que aporta este documento

- Registro de la decision de mantener un flujo simple para `profile-site`.
- Contexto de por que se integraron `tdd` e `improve-codebase-architecture` dentro del flujo OpenSpec.
- Contexto de la estrategia de overlays locales (`ospec-*`) frente a comandos upstream (`opsx-*`).

## Regla de mantenimiento

- No duplicar aqui reglas operativas que ya esten en el flujo canonico.
- Si se actualiza la operativa, modificar primero `docs/workflow/openspec-simple-skills-flow.md`.
